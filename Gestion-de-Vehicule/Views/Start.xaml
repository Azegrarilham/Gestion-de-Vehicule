<Window x:Class="Gestion_de_Vehicule.Views.Start"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Gestion_de_Vehicule.Views"
        mc:Ignorable="d"
        Title="Gestion de Véhicules"  
        Height="600" 
        Width="1000"
        WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize">
    <Window.Background>
        <ImageBrush ImageSource="/background.png" Opacity="1"/>
    </Window.Background>
    <Window.Resources>
        <!-- Modern Styles -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16 10"/>
            <Setter Property="Margin" Value="0 0 10 0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                             CornerRadius="8"
                             BorderBrush="{TemplateBinding BorderBrush}"
                             BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                           VerticalAlignment="Center"
                                           Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Shadow Effect Resource -->
        <DropShadowEffect x:Key="ShadowEffect" BlurRadius="0" ShadowDepth="0" Color="#20000000"/>
    </Window.Resources>
    <Grid>
        <TextBlock
            Text="Gestion de Véhicules"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Margin="0,100,0,0"
            TextWrapping="Wrap"
            FontSize="72"
            FontFamily="Arial Black"
            Foreground="#FFFDC742">

            <TextBlock.Effect>
                <DropShadowEffect
                Color="#FF363535"
                BlurRadius="5"
                ShadowDepth="3"
                Opacity="1" Direction="284"/>
            </TextBlock.Effect>
        </TextBlock>
        <TextBlock Text="Gérez votre flotte de véhicules" FontSize="36" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,170,0,0" FontFamily="SimSun">
            <TextBlock.Effect>
                <DropShadowEffect
                  Color="Black"
                  BlurRadius="5"
                  ShadowDepth="5"
                  Opacity="1"/>
            </TextBlock.Effect>
        </TextBlock>
        <Button Style="{StaticResource ModernButton}" Content="Start >>" HorizontalAlignment="Center"  VerticalAlignment="Center" Margin="0,0,10,0" FontSize="36" Width="150" Background="#FF08B9E9" FontFamily="Bahnschrift" Height="55" FontWeight="Medium" Click="Button_Click"/>
    </Grid>
</Window>
